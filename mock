def removeDuplicate(head):
    current = head
    
    while current and current.next:
        if current.data == current.next.data:
            current.next = current.next.next  # Skip the next node (remove duplicate)
        else:
            current = current.next  # Move to the next node
    
    return head
