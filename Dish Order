def DishPrepareOrder(order_list):
    dish_menu = [1001, 1002, 1003, 1004, 1005, 1006, 1007, 1008, 1009]

    dish_counts = {}
    for dish_id in order_list:
        if dish_id in dish_counts:
            dish_counts[dish_id] += 1
        else:
            dish_counts[dish_id] = 1

    prepare_order = sorted(dish_counts.keys(), key=lambda x: (-dish_counts[x], x))

    return prepare_order
nums = eval(input())
print(DishPrepareOrder(nums))